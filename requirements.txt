# Core
torch>=2.10.0
torchvision

# Detectron2 — install separately (not on PyPI):
#   pip install --no-build-isolation git+https://github.com/facebookresearch/detectron2.git
# Mask2Former — clone separately, then copy in mask2former_patches/ (see environment_setup.sh)
#   git clone https://github.com/facebookresearch/Mask2Former.git

timm            # provides vit_base_patch14_dinov2 for the DINOv2 backbone
einops
scipy
opencv-python
pycocotools
shapely
numpy
Pillow
tensorboard     # SemanticMonitor writes TB logs
